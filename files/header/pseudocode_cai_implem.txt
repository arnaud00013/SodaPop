PSEUDO-CODE mutations (à faire ds chaque fonction de mutations!?!):

-rajouter paramètre p_is_simulating_CUB_effect à la fonction
-Dans la fonction de mutation avant initialisations, rajouter:
    if (simulCub_){
        float old_cai = this->get_cai()
    }

-Dans la fonction, dans le bloc de qd il y a synonymous mutation:
    if (simulCub_){
        #APRÈS LA MUTATION
        this->update_cai(); 
        double new_cai = this->get_cai()
        draw un double th#e_s_coef de RandomNormal
        si new cai est plus grand que old cai, re-draw tant que the_s_coef n'est pas >0, 
        sinon si new cai est plus petit que old cai, re-draw tant que the_s_coef n'est pas <0, 
        update s avec the_s_coef!!!
        (bloc original)
        
    }sinon{
        (bloc original)
    }

